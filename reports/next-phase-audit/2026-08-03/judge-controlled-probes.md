# Controlled Judge probes

No controlled request was executed. Cost: CNY 0 / USD 0.

A user whose masked name matched a test pattern exists and owns two active model API Tokens, but neither account remark nor Token name marks them as dedicated test/audit credentials. The user has no Dashboard access token and no reusable browser session was found. Using these Tokens could affect an existing user's Task/Session and would violate the audit's safety condition. No Profile was paused, no health state was changed, and no Probe was enqueued.

Existing production evidence supplies New Judge (`new_task`, `human_message`, `plan_started`), Reuse and failover samples without manufacturing failures.
